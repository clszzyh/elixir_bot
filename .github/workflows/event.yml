name: event

on:
  workflow_dispatch:
  issue_comment:
  issues:

jobs:
  exbot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: elixir_bot
        uses: ./
        with:
          token: ${{secrets.PERSONAL_TOKEN}}
