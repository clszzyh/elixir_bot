name: event

on:
  workflow_dispatch:

jobs:
  exbot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: elixir_bot
        uses: ./
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          input: ${{ secrets.GITHUB_TOKEN }}
